name: weather
description: A weather application with beautiful UI and animations.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  intl: ^0.18.1
  geolocator: ^10.0.0
  flutter_dotenv: ^5.1.0
  google_fonts: ^5.1.0
  provider: ^6.0.5
  shimmer: ^3.0.0
  flutter_animate: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
